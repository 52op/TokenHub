{
  "name": "tokenhub",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute tokenhub-db --file=schema.sql",
    "db:remote": "wrangler d1 execute tokenhub-db --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
